---
description: 根据已有规范压缩当前对话上下文，并自动归档进度与踩坑记录致文件 (Context Compression & Progress Save)
---

# 阶段 1：回顾与高密度压缩 (Review & Density Compression)
仔细审阅当前会话的完整历史轨迹，提取关键信息并分类整理。

# 阶段 2：更新工作进度档案 (Update Progress)
读取 `E:\STM32\OV\docx\07_Current_Progress.md` (如果存在)，合并新进展后，**覆盖更新**该文件。强制要求按以下结构写入内容：
- **当前任务目标**：本次对话在做什么，最终要达成什么
- **已完成步骤**：按时间顺序列出已完成的工作，附带关键结果
- **未完成计划**：还有哪些待办事项，按优先级排列
- **关键决策及原因**：做过的重要技术/架构决定，以及为什么这样决定（例如 DWCAF 放弃纯空间注意力改为余弦交叉注意力）
- **当前实验状态**：如有训练/实验在跑，记录模型名、当前 epoch、最新指标等情况
- **关键文件路径**：涉及的重要文件，按组件分类列出
- **当前判断与下一步**：对当前进展的评估，以及建议的后续方向

# 阶段 3：更新踩坑记录 (Update Pitfalls)
检查本轮对话中是否遇到了以下情况（如：调试超过 2 轮才解决的问题、环境坑、非预期计算错误如 NaN/Inf 等）：
如果遇到，预读取 `E:\STM32\OV\docx\08_Pitfalls.md` (如果存在)，并将新增坑位采用以下格式**追加/更新写入**该文件：
```markdown
### [日期] 简要标题
- **现象**：出了什么问题（如 train/angle_loss NaN，map50 变 0）
- **原因**：根本原因是什么（如 FP16 大规模特征累乘引发数据溢出）
- **解决**：怎么修的（引入 F.normalize 防止数值炸裂）
- **教训**：下次设计复杂计算或激活函数时，警惕混合精度的阈值效应。
```

# 阶段 4：导师模式收尾 (Graceful Shutdown & Continue Setup)
向用户输出汇报内容：
1. 明确声明**工作进度和踩坑日志均已保存**。
2. 列出关键摘要（3-5 行，囊括更新的重点与新写入雷区的精简标题）。
3. 主动建议用户：如果您感觉当前窗口略有卡顿，可以随时新开对话。新开对话时不仅协议会自动拦截阅读进度，您也可以随时输入指令 `/continue` 来让进度快速重置载入。
4. 严格遵守导师设定结尾：“喵~”。
